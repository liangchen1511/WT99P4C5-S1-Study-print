---
description: WT99P4C5-S1 工程改动标准 — 每次优化必须同步更新 CHANGELOG
alwaysApply: true
---

# WT99P4C5-S1 工程改动标准

## 改动文档（必须）

每次代码或配置改动，**必须**在 [`docs/OPTIMIZATION_CHANGELOG.md`](../docs/OPTIMIZATION_CHANGELOG.md) 追加一条记录。

每条记录必填：

| 字段 | 说明 |
|------|------|
| 阶段编号 | 如 `[阶段1.1]` |
| 改了什么 | 一句话摘要 |
| 改动位置 | 文件路径 + 行号/函数名 + 变更说明 |
| 原因 | 为何改动 |
| 预期影响 | Flash / PSRAM / Internal RAM / 性能 |
| 验证 | build、size、功能回归清单 |

## 配置改动

- **持久化配置只改 defaults**：`sdkconfig` 在 `.gitignore` 中，不提交；任何需保留的 `CONFIG_*` 必须同步写入 `sdkconfig.defaults` 和/或 `sdkconfig.defaults.release`（`CMakeLists.txt` 按此顺序 merge）。
- **分工**：全构建共用 → `sdkconfig.defaults`；仅 Release 覆盖（日志/字体/`-Os` 等）→ `sdkconfig.defaults.release`（后加载，覆盖前者同名项）。
- **本地试验**：可只改 `sdkconfig`；确认后 `idf.py save-defconfig` 或手工同步到上述 defaults，必要时删 `sdkconfig` 后 `idf.py build` 验证 merge。
- **CHANGELOG**：`sdkconfig` / `sdkconfig.defaults*` 变更须写明 `CONFIG_*` 键名及 **旧值 → 新值**，并列出实际修改的 defaults 文件。

## 资源删除/排除

列出被删或 CMake 排除的文件名，并注明预估 Flash 节省。

## 阶段验证

每阶段完成后运行 `idf.py build` 与 `idf.py size`，将结果写入 CHANGELOG「验证记录」节。

详细说明见 [`docs/ENGINEERING_STANDARD.md`](../docs/ENGINEERING_STANDARD.md)。
